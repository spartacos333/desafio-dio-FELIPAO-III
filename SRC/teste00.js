// Pretendo simular uma rodada de ataque de uma equipe em um jogo de RPG.
// Nesse teste, meu objetivo principal foi criar um código funcional com as bases para a ideia inicial.
// Para o próximo teste, pretendo codificar em inglês.
let hero = {
    name : "Spartacos333",
    age : 10,
    class : Mage = {
        ataque : "Magia"
    }
  };
  console.log ( `O ${hero.name} atacou usando ${hero.class.ataque} de class !`);
 hero.localizacao = "Salao principal";
  console.log ( `${hero.name}  está no ${hero.localizacao} .`);
/*  Saída:
  O Spartacos333 atacou usando Magia de class !
  Spartacos333  está no Salao principal .
*/